apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: frontend
  namespace: hipster-shop
spec:
  hosts:
  - "*"
  gateways:
  - hipster-gateway
  http:
  - route:
    - destination:
        host: frontend
        port:
          number: 8080
